    import XCTest
    @testable import RDSpreadsheet

    final class RDSpreadsheetTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(RDSpreadsheet().text, "Hello, World!")
        }
    }
